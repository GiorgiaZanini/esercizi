/*-) dichiari una matrice di nome Matrix, di dimensione NxM (dove N e M sono definiti a inizio programma come N=2, M=3), di numeri interi;

-) dichiari due vettore di interi di nome SumR e SumC di dimensione N e M rispettivamente;

-) inizializzi la matrice t.c. le righe pari (incluso riga 0) abbiano numeri random pari e le righe dispari abbiano numeri random dispari, 
sia pari che dispari compresi tra 0 e 10 (si ricorda che 2*x che è sempre un numero pari e che 2*x+1 è sempre un numero dispari);

-) implementi la somma per righe e per colonne salvando i risultati in SumR (SumR[0] conterrà la somma degli interi di Matrix riga 0, 
SumR[1] la somma di riga 1 etc.) e SumC (SumC[0] conterra la somma degli interi di Matrix colonna 0, SumC[1] la somma di colonna 1, etc.);

-) stampi Matrix e Sum.

Matrix:

2 2 10
3 7 3

SumR:

14 13

SumC:

5 9 13 */


#include <stdio.h>
#include <stdlib.h>
/*
 ulteriori direttive di pre-processing
*/

int main() {
/*
dichiarazioni
*/
srand(1);
/*
inizializzazione Matrix:
*/

/*
somma per righe
*/
    
/*
somma per colonne
*/
    
/*
stampe di Matrix, SumR e SumC
*/

    
  return 0;
}

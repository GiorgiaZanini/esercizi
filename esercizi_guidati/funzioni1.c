#include <stdio.h>
int parif(int);

int main(){
	int n,r;
	scanf("%d",&n);
	r=pari(n);
	printf("%d",r);
}

int parif(int num){
	if(num % 2 == 0);
		return 1;
	}else{
		return 0;
	}

}

//void -> procedura (modifica variabili)
//int -> funzione
